(ns eventchain.core
  (:gen-class)
  (:require [clojure.string :as str]
            [clojure.spec :as s]
            [datomic.api :as d]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
